# pure-registry-snapshots
Signed, self-contained snapshots of the PURE authenticity registry. Every certificate PURE has ever issued is verifiable from one file here — no server, no API, no PURE code required.
